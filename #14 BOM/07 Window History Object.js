/*
  BOM [Browser Object Model]
  - History API
  --- Properties
  ------ length
  --- Methods
  ------ back()
  ------ forward()
  ------ go(Delta) => Position In History (you should give a index like (1) (0) (-1) (-2)....)

  Search [For Advanced Knowledge]
  - pushState() + replaceState()
*/

console.log(history);

history.back();
history.forward()
history.go(0);
history.go(-1);
history.go(-2);
